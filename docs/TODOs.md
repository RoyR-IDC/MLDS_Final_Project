# TODOs

- for all notebooks in "/Users/royrubin/Documents/GitHub/MLDS_Final_Project/src/notebooks/"
    -  structure: (before each)


- regarding the notebook "/Users/royrubin/Documents/GitHub/MLDS_Final_Project/src/notebooks/data_and_loaders.ipynb"
    - 


major update:
code is expected to run only in 3 notebooks - part1,2,3 - and not through scripts.
for better and cleaner notebooks it will be best that most of the functions and functuinailty will be called from files in utils or training folders etc (local imports).

regarding notebooks
- structure: 
    - each notebook should have the following structure (each of the following bullets means a code cell, and before it a markdown cell explaining it)
        - introduction and notebook topic explanation
        - global or external imports
        - local imports
        - global definitions
        - data loading
        - experiments 
            - make experiment sub sections as much as needed

regarding configs
- verify no double configs
- aggregate configs into sections such as general, models, experimets, input output, etc.

general notes
- variable names should be readable
- use classes where possible
- use comments generously